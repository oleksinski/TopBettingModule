<div>
    {{> tabs}}
</div>

<script type="text/javascript">
    $(function() {
        $('#tabs li').each(function(i) {
            $(this).on('click', function (e) {
                var tab = $(this).find('a').data('tab-id');
                var sport = 'football';
                getContent(tab, sport);
            });
        });

        getContent('15mins', 'all_sports');
    });

    function getContent(tab, sport) {
        $.ajax({
            url: "/" + tab + "/" + sport + '?r=' + Math.random(),
            context: document.body
        }).done(function(data) {
            pasteContent(tab, sport, data);
        }).fail(function( jqXHR, textStatus ) {
            pasteContent(tab, sport, jqXHR.responseText);
        });
    }

    function pasteContent(tab, sport, content) {
        console.log(content);
        var tab_id = "tab_content_" + tab;
        $('#'+tab_id).append(content);
        console.log($('#'+tab_id));
    }

</script>